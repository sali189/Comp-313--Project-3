Syed Zain Ali

				READ ME FILE

1. the linked list is faster because it dynamically allocates the size to grow. Arrays need to be defined before, however. 
2. List.remove(5) removes 5 from the list. (as stated below)
3. list.remove(Integer.valueOf(5)) removes the 5th value of the element. 
4. list.remove simply removes the integer parsed in it.

Array size 10: 0.215s

Array size 100: 0.29s

Array size 1000: 1s

Array size 10000: 9s